# AstraDB configuration
ASTRA_DB_ID=your_astra_db_id
ASTRA_DB_REGION=your_astra_db_region
ASTRA_DB_APPLICATION_TOKEN=your_application_token
KEYSPACE_NAME=your_keyspace_name

# LangFlow configuration (optional)
LANGFLOW_API_URL=http://localhost:8000
LANGFLOW_API_TOKEN=your_langflow_token
FLOW_ID=your_flow_id

# Server port (optional)
PORT=3001

# Notes:
# - Copy this file to '.env' and fill the values before starting the backend.
# - The backend's embedding script path is currently hardcoded in server.js. You may
#   need to update that path or place the Python script at the referenced location.
